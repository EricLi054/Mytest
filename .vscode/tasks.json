{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run GraphQL Gateway (Local)",
      "type": "shell",
      "command": "powershell",
      "args": ["-File", "run-supergraph-gateway.ps1"],
      "options": {
        "cwd": "${workspaceFolder}/backend"
      }
    }
  ]
}
