'use client'

import { FontAwesomeIcon as FAIcon, type FontAwesomeIconProps } from '@fortawesome/react-fontawesome'

function FontAwesomeIcon(props: FontAwesomeIconProps) {
  return (
        <FAIcon {...props} />
  )
}

export default FontAwesomeIcon
