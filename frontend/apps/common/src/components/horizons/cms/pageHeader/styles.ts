export const styles = {
  pageHeaderSection: (sectionColour: string) => ({
    backgroundColor: sectionColour === "White" ? "#FFFFFF" : "#F3F3F3",
    py: 8,
  }),
  pageHeaderImageContentContainer: {
    position: "relative",
    zIndex: 2,
    width: "100%",
  },
};
